[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"

requests = { version = "^2.32.2", optional = true }
pre-commit = { version = "==2.4.0", optional = true }

[tool.poetry.extras]
group1 = ['requests']
group2 = ['pre-commit']


